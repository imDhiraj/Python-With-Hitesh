this_depending_value=True
current_value_of_items= 5

total_amount_values= current_value_of_items+this_depending_value 
print(f"total value {total_amount_values}")
#upcasting :it convert required datatype 

# following two types represent how we can repersent the boolen value 

persent_count_of_milk = 1 # 11 "hitesh"
print(f"the current value  `{bool(persent_count_of_milk)}`")

if_value_is_not_there= 0 # none
print(f"if the value is not avlible `{bool(if_value_is_not_there)}`")


# boolen can be use logical opration 

hot_water = True
tea_mix=False

print(f"Tea perpare to serve or not ? \n {bool(hot_water and tea_mix)}")

